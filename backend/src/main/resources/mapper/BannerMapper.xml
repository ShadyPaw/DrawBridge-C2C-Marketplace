<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cargoco.mapper.BannerMapper">

    <resultMap id="BaseResultMap" type="com.cargoco.entity.Banner">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="image_url" property="imageUrl"/>
        <result column="link_url" property="linkUrl"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM banner WHERE id = #{id} AND deleted = 0
    </select>

    <select id="findActive" resultMap="BaseResultMap">
        SELECT * FROM banner WHERE deleted = 0 AND status = 1 ORDER BY sort_order ASC
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM banner WHERE deleted = 0 ORDER BY sort_order ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banner (title, image_url, link_url, sort_order, status)
        VALUES (#{title}, #{imageUrl}, #{linkUrl}, #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE banner
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE banner SET deleted = 1 WHERE id = #{id}
    </update>
</mapper>
